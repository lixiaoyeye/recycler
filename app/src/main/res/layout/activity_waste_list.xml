<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <include layout="@layout/common_header_layout" />
    
    <include layout="@layout/date_pageturn_layout"/>

    <health.rubbish.recycler.widget.xlist.XListView
        android:id="@+id/waste_listview"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:cacheColorHint="@android:color/transparent"
        android:divider="@null"
        android:dividerHeight="0dp"
        android:listSelector="@android:color/transparent"></health.rubbish.recycler.widget.xlist.XListView>

    <Button
        android:id="@+id/upload_btn"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:gravity="center"
        android:background="@color/common_blue"
        android:padding="10dp"
        android:text="上    传"
        android:textColor="@color/white"
        android:textSize="20sp" />
</LinearLayout>